image: mono:latest

stages:
    - build

variables:
    solution: "SerialKiller.sln"

before_script:
    - nuget restore

build:
    stage: build
    script:
        - msbuild /p:Configuration=Release $solution